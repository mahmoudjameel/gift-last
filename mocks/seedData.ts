import { Order, SavedAddress, WalletTransaction, AppNotification, Conversation, ChatMessage, User } from '@/types';

export const seedUser: User | null = null;

export const seedStoreInfo = null;

export const seedOrders: Order[] = [];

export const seedCart: any[] = [];

export const seedAddresses: SavedAddress[] = [];

export const seedWalletBalance = 0;

export const seedWalletTransactions: WalletTransaction[] = [];

export const seedWalletPending = 0;

export const seedFavoriteIds: string[] = [];
export const seedFavoriteStoreIds: string[] = [];

export const seedNotifications: AppNotification[] = [];

export const seedMerchantConversations: Conversation[] = [];

export const seedCustomerConversations: Conversation[] = [];
